class Solution{
    MissingNumber(array,n){
        //code here
        let i,sum=0;
        let nsum ;
        nsum = (n*(n+1))/2;
        for(i=0;i<n-1;i++)
        {
            sum=sum+array[i];
            
        }
        nsum =nsum-sum
        return nsum
    }
