 let i,sum=0;
       arr.sort(function(a, b){return a - b})
       for(i=0;i<n;i++)
       {
           if(arr[i]<0 && k>0)
           {
           arr[i]=-arr[i];
           k--;
           }
       }
        arr.sort(function(a, b){return a - b})
        if(k%2!==0)
       {
           arr[0]=arr[0]*-1
       }
       for(i=0;i<n;i++)
       sum=sum+arr[i];
       return sum
